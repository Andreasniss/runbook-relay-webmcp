interface:
  display_name: "Evidence SDLC"
  short_description: "Deliver scoped changes with verifiable evidence"
  default_prompt: "Use $evidence-sdlc to implement this change under the repository's existing rules and verify the candidate revision."
